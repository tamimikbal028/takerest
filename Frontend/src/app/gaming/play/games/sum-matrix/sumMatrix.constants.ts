export const DEFAULT_POINTS_PER_CORRECT = 20;
export const DEFAULT_PENALTY_PER_WRONG = 5;
export const DEFAULT_MAX_SKIPS = 3;
export const DEFAULT_MAX_MISTAKES = 3;
export const DEFAULT_TOTAL_QUESTIONS = 10;

export const GAME_INSTRUCTIONS =
  "Rearrange numbers in the grid to satisfy the row and column target sums. Click a cell, then click an adjacent cell to swap them.";
